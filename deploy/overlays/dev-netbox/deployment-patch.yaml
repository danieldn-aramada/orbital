apiVersion: apps/v1
kind: Deployment
metadata:
  name: orbital
spec:
  template:
    spec:
      containers:
        - name: orbital
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: orbital-secrets
                  key: DATABASE_URL
