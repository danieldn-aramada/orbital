apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: netbox
resources:
  - ../../base
  - secrets.yaml  # gitignored — copy from deploy/dev/secrets.yaml (kustomize sets namespace: netbox)
images:
  - name: armadaeksatest.azurecr.io/orbital
    newTag: v0.0.15

patches:
  - path: deployment-patch.yaml
# Secrets are NOT managed here — apply separately:
#   kubectl apply -f deploy/dev/secrets.yaml -n netbox
