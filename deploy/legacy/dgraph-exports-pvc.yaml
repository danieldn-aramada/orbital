apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orbital-dgraph-exports
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 5Gi
