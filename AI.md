# AI Usage Log

This file is a minimal audit log of AI assistance used during development. It records when AI was used, which model, and the scope — not the full narrative. Architectural decisions, working conventions, and "do not suggest" rules live in `CLAUDE.md`, which is the source of truth for AI behavior in this repo.

AI was used as a collaborative engineering partner, not a code generator. All architectural decisions were made by the human engineer.

## Log

| Date     | Model                | Scope                                      |
|----------|----------------------|--------------------------------------------|
| 2026-04  | claude-sonnet-4-6    | Architecture, scaffold, roadmap            |
| 2026-04  | claude-sonnet-4-6    | Orb CLI scaffold and output UX             |
| 2026-05  | claude-sonnet-4-6    | SDD v0.3 review, project boundary, configbundle integration, roadmap and MVP definition, reporting auth architecture (transport-agnostic intake API, orb identity + Ed25519 signing, orbs PostgreSQL registry), deployment-generic terminology cleanup |
| 2026-05  | claude-sonnet-4-6    | UI: datacenter tab (htmx, htmx:after-swap inner tab wiring, load-once tab caching with dataset.loaded, skeleton, Grafana todo button, overflow fix, cache busting); Playwright E2E tests; example graphql seeds parsed from xlsx (seattle-galleon, houston-galleon, alaska-dot-galleon) |
| 2026-05  | claude-sonnet-4-6    | Spike 8 (backup): async DGraph backup to S3/Azure Blob, checksum dedup, retention, presigned download, test connection, `orbital-<version>-<timestamp>.zip` naming; Spike 6 partial (export + OCI): subgraph export API with blue-green DGraph, per-job scratch dirs via `destination` param, global job serialization, OCI artifact publish (oras-go v2 + cosign, air-gap safe), Edge Delivery UI, swagger annotations, `make docs` target |
| 2026-05  | claude-sonnet-4-6    | Schema: `IPAddress` as first-class `ConfigItem` with typed hub back-refs (`serverOobIP`, `eksaConfigTinkerbellIP`, `eksaControlPlaneIP`) — settled GraphQL-only hub pattern after evaluating DQL `usedBy` and interface-typed `assignedTo` (both rejected: DGraph `@hasInverse` requires concrete types, not interface targets); DQL `~predicate` pattern documented for cross-type IP queries. `KubernetesCluster` + `EksaConfig` types added. `id: ID` added to `ConfigItem` interface (required for `getDataCenter(id: $id)` queries). Seed data: real hostnames + rack positions from Netbox for alaska-dot-cruiser (G8-Rack1/G8-Rack2) and alaska-dot-galleon. UI fixes: app version display (embedded struct field shadowing bug in `page.Home`), DC detail tab state cleared on tab close. Playwright E2E: global auth setup, `data-testid` convention, dynamic server count assertion. `make test-e2e` target. |
| 2026-05-11 | claude-sonnet-4-6  | orbital-cli scaffold; OIDC Auth Code + PKCE login flow (Device Code rejected — Conditional Access policies can block it); credential storage split: keychain stores `{refresh_token, name, email}` JSON blob via `github.com/zalando/go-keyring`, access token written to `~/.orbital/credentials.json` (0600) — Azure AD JWTs ~6KB exceed go-keyring's 4096-byte macOS `security -i` limit; subcommands read file only, never refresh silently; `internal/orbauth/` shared auth package (PKCE, token exchange, refresh, Store interface, FileStore, KeychainStore) used by both `orb` and `orbital-cli`; `build-orbital-cli` Makefile target. |
| 2026-05-11 | claude-sonnet-4-6  | Spike 8 (authorization) progress: macOS keychain rewritten to CGo + Security framework directly (`SecItemAdd`/`SecItemCopyMatching`/`SecItemDelete`), dropped Touch ID ACL (`errSecMissingEntitlement -34018` requires Apple code signing entitlements — not viable for unsigned CLI), uses `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`. Bearer token validation end-to-end: `go-oidc/v3` validates Azure AD v2 JWTs via JWKS discovery; Azure AD app manifest fix (`requestedAccessTokenVersion: 2`); audience fix (v2 `aud` = bare client GUID, not `api://<guid>`); `/api/v1/graphql` registered on bearer-protected `api` group. `orbital get datacenter <name|orbId|id>` + `orbital get datacenters` CLI commands — bearer auth, kubectl-style dynamic column widths. New seed data: 2f-uae (Site 30, 2 racks, 6 servers), grayling-galleon (Site 18, 13 servers), livermore-galleon (Site 32, 19 servers). All `createdAt` dates normalized to `2026-05-11T00:00:00Z`. |
| 2026-05-11 | claude-sonnet-4-6  | UI: Servers config domain — cross-DC Servers page with DataTable (Data Center, OOB IP, Hostname, Service Tag, Model, Rack); server detail view with drill-down from DC tab (double-click row → server detail replaces DC tab content; back button restores DC); server detail tabs (iDRAC settings, Storage controllers/devices, Config Profile JSON); server edit modal (JSONEditor, all scalar fields, post-save context-aware reload via `data-reload-url`/`data-reload-target`); tab persistence for Servers page (`localStorage.serverTabs`). Settled patterns: HTMX window bridge for ES modules (`window.JSONEditor` in head.gohtml), lazy JSONEditor init on Edit click (not on swap — hidden container produces blank editor), GraphQL HTTP 200 even on errors (two-check pattern: `resp.ok` + `result.errors`), DataTables Bulma select wrapper (`dtWrapLengthSelect()`). Seed data: all servers updated with `manufacturer: "Dell"` and Redfish model names (`PowerEdge R650`, `PowerEdge XE9680`, etc.). Field ordering convention: Data Center first, then alpha for server summary. |
| 2026-05-13 | claude-sonnet-4-6  | Audit log polish: before/after diff rendering in audit event rows — server-side LCS line diff (`buildDiffHTML` in `event.go`, `diffableFields` map per resource type); before-state fetched in GraphQL proxy before mutation, stored in `details["before"]`, diffed against mutation variables on render; HTTP response body drain fix in `doFetch`/`fetchBeforeByOrbID` for keep-alive reuse. DC edit save reload fixed: replaced `htmx.ajax()` with plain `fetch()` + manual `innerHTML` + direct `initDcDetailTabs()` call — `htmx.ajax()` carries hidden request context that misroutes responses when called imperatively from async handlers (audit panel showed servers table content after edit save). Settled convention: use `fetch()` for all programmatic tab reloads, never `htmx.ajax()`. Startup log fixed: `cmd/orbital/main.go` now calls `slog.SetDefault` before first log line so startup message emits JSON consistent with all other output. |
| 2026-05-14 | claude-sonnet-4-6  | Spike 17 (restore): full restore-from-S3 feature — `restore_jobs` ent table; `POST /api/v1/restore` (blocked if backup/export/restore in progress); restore job runner downloads backup zip from S3 to shared PVC, execs `drop_all` against DGraph Alpha, execs `dgraph live` in a dedicated `dgraph-live` idle pod (`deploy/dev/dgraph-live.yaml`, `deploy/dev/orbital-restore-pvc.yaml`); `client-go` `InClusterConfig()` with `k8sAvailable` fallback; RBAC (`deploy/dev/rbac.yaml`); `ORBITAL_RESTORE_TIMEOUT` env var; Restore UI page with job table, backup selector, and manual runbook. End-to-end validated on AKS. AKS export fix: `backups.full.enabled: false` in `values-dev-scratch.yaml` silently removed the `orbital-scratch-exports` PVC mount — fixed with `enabled: true` + never-firing cron (`"0 0 31 2 *"`). OCI registry display: export and signed-artifacts pages now show `{registry}/{repo}/{datacenter-name}`. Divergence reports description corrected. Server summary table: `is-size-7`, `pageLength: 50`, smaller pagination. Dead code cleanup: `loadServerTable()` and two dead `summary.gohtml` templates deleted. IPAddress seed fix: missing `name` field added to livermore and grayling. Favicon: FA6 `satellite-dish` SVG data URI with white rounded-rect background. `scripts/seed-graphql.sh` added. |
| 2026-05-15 | claude-sonnet-4-6  | AKS deployment fixes and UI polish: fixed `ORBITAL_EXPORT_DIR` not set in deploy.yaml (export zips written to ephemeral container storage, lost on pod restart — fixed to `/scratch-exports/zips` on existing PVC); fixed `BASE` path double-prefix bug on AKS (`data-srv-url` in server-tab.gohtml included `{{.BasePath}}` while JS also prepended `BASE`); fixed `modal.dataset.reloadUrl` missing `BASE` prefix (server edit save → 404 on AKS). UI: page titles changed to `<Page> | Orbital` pattern; inventory table sessionStorage cache + `searchCols` pre-filter (eliminates ajax flash and filter redraw on revisit); `stateSave: true` on all main DataTables; inventory type filter persisted to localStorage; server/DC/audit page length menus updated; navbar Design link removed (broken). Seed: `seed-dgraph.sh` two-pass ordering (base files before supplementary); `seed-aks.sh` port-forwards scratch Zero; `seed-aks-postgres.sh` new script. Settled: PostgreSQL backup is out-of-band (managed service), post-MVP to coordinate with DGraph backup; restore scope is DGraph only for MVP. Spike 16 complete (all iDRAC seed data for all 9 data centers, 4 new schema fields). |
| 2026-05-21 | claude-sonnet-4-6 + claude-opus-4-6 | Spike 13 + 17 completion: orb override system (field-level overrides to `overrides.json`, Server + iDRAC fields, override badges, import warning, Divergence Report page with Publish button); orb UI fixes (server tab HTMX tab pattern matching orbital, DataTables DC list, reload skeleton variant for orb vs orbital); footer bug fix (`AppName` was set to DCSlug, corrected to `"Orb"`); product naming settled: "Orbital" (cloud) / "Orb" (edge) — north star decision documented in CLAUDE.md. Opus design sessions: override system invariants (DGraph read-only mirror, import-is-sudo, air-gap transport), override modal sustainability analysis (data-attribute pattern debt), session efficiency retrospective (page behavior specs missing from spike plans), branding analysis (Orbital vs Orbital Edge vs Orb). |
| 2026-05-22 | claude-sonnet-4-6 + claude-opus-4-6 | Spike 17 completion + orb UI additions: orb inventory page (`/inventory`, reuses `handler.Inventory` DQL query + same DataTable as orbital); orb schema page (`/schema`, `importer.go` now saves decompressed SDL to `{DataDir}/schema.graphql` on import); `DCSlug` removed from `orbconfig.Config` — orb is now stateless re: DC identity, DC name derived from imported DGraph data, `ORB_OCI_REPO` carries full DC path; override system removed (orb is read-only; divergence/override work deferred to ConfigBundle domain + Spike 14); orbital inventory namespace filter — page-level scope selector above DataTable (not in toolbar), regex search on orbId column (`^namespace:`), persisted to localStorage. Opus: namespace filter UX design — namespace is scope not column filter, settled on page-level selector. |
| 2026-05-24 | claude-sonnet-4-6 | Spike 19 (ConfigBundle enricher integration): Orbital made sole OCI producer — enricher model replaces webhook design; `internal/enricher/` package with `ClientOption` functional options (`WithHTTPClient`, `WithMaxResponseBytes`); `go-retryablehttp` backing HTTP client; `ORBITAL_ENRICHER_TIMEOUT/MAX_ATTEMPTS/MAX_RESPONSE_BYTES` config vars; per-request enricher URLs in publish body; all-or-nothing enrichment before push; `enriched`/`enricher_error` fields on `RegistryArtifact` ent schema; Enriched column on Signed Artifacts UI; enricher unit tests (12 cases, httptest fake server); `docs/configbundle-integration.md` full integration plan for ConfigBundle repo. Bug fixes: orb e2e navbar test strict mode violation (`.first()`); `orb.spec.ts` excluded from default playwright config (`testIgnore`); `exec_test.go` wrong container name (`orbital-` → `local-`). |
| 2026-05-18 | claude-sonnet-4-6  | UI: Ratel navbar link — todo toast sentinel (`RATEL_URL="#"`) for AKS (Ratel React SPA with `PUBLIC_URL=/` has webpack-baked absolute paths; sub-path proxy is fundamentally broken; correct fix is dedicated DNS hostname + Istio VirtualService, documented as settled decision in CLAUDE.md); toast polish (warning style, icon-first message, no X button, matching font size); hint banners on datacenter and server pages (sessionStorage dismissal per page key). ROADMAP: full restructure — split completed checklist history into new CHANGELOG.md; ROADMAP trimmed to gantt + "What We've Built" summary + in-progress sections + MVP Planning; spikes renumbered sequentially 1–14; production deployment spike closed and its items redistributed to MVP Planning (infra team input required); security/testing/perf/observability collapsed into MVP Planning sections. |
| 2026-05-12 | claude-sonnet-4-6  | Audit log eventing system: `events` PostgreSQL table (ent schema, no `Immutable()` — enforced at app layer); GraphQL proxy middleware intercepts all mutations touching known ConfigItem types via regex (`knownMutationRe`); one event per HTTP request — compound mutations (aliases) produce one row; `operations []string` stores extracted DGraph op names (e.g. `["updateDataCenter","updateServer"]`); `resource_types []string` and `resource_ids []string` extracted from query body (regex) and variables; full raw payload `{operationName, query, variables}` in `details` jsonb. MVCC (`ifVersion`) decoupled from eventing — events always recorded, MVCC opt-in. Audit log UI: standalone page (DataTable, expand child row shows formatted GraphQL + variables); per-entity audit tab on DC and server detail views. `make seed` now applies schema to both blue (`:8080`) and scratch (`:8081`) DGraph instances. Logout clears `localStorage` and `sessionStorage`. Tech debt noted: `vektah/gqlparser` AST parsing preferred over regex long-term — tracked in ROADMAP.md technical debt. Design decisions documented in `docs/decisions/001-mutation-audit-recording.md`. |
