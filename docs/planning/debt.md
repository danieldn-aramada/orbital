# Known debt

Tracked technical debt for orbital. Split out of `ROADMAP.md` on 2026-08-28 — it had grown to 42 entries (one of them 2.5 KB), which buried the milestone view that ROADMAP exists to give.

**This is an interim home.** By the convention of every comparable project (Kubernetes, Rust, Django, Terraform), debt belongs in the issue tracker with labels, not in a file. Move it there when the workflow is ready; until then, this file keeps it out of the roadmap.

Entries are grouped by whether they need design input before someone can pick them up.

---

**Validation gate — before closing any item:** `go build ./...` + `make test-unit` for Go changes; `make test-integration` for PostgreSQL/DGraph; `make test-e2e` or manual browser check for UI/JS; negative test (wrong config is actually rejected) for security-critical items.

### Track A — fix now (no test harness required)

| Item | Notes |
|---|---|
| Replace `title=""` tooltips with Tippy.js | 9+ usages in `divergence-reports.gohtml`, `users.gohtml`, `backup-jobs-tbody.gohtml`, `cluster-tab.gohtml`. Native tooltips have ~1s delay, no theming/positioning. Migrate to Tippy.js (~10KB). |
| Refactor bundler URL config DSL | `ORBITAL_BUNDLER_URLS=configbundle-bundler=http://...` is a custom micro-DSL in one env var; already caused one bug (preflight probed the raw `name=url` as a URL). Better: one env var per bundler, or ConfigMap-mounted YAML. |
| Collapse duplicate cluster backup structs | `backupKindResponse` (DGraph decode) and `backupKindTab` (template view struct) are hand-synced — adding a field means updating both or the cluster tab truncates mid-render (the 2026-07-27 `retentionDays` bug). Now *caught* by `TestClusterTab_BackupWithRetentionRendersFullFragment` + buffered `renderHTML`, but drift is still possible. Render the template off the query struct directly so a new field lands in one place. `internal/handler/cluster.go`. |
| Unify `/graphql` proxy-guard error envelope | **Deferred — build only when a client needs it.** Proxy guards (403/409/400) return the REST `errorResponse` body; DGraph's native `errors[]` passes through untouched, so a caller gets one shape or the other. Unify = wrap proxy-guard rejections in a synthetic `errors:[{message, extensions:{code,httpStatus,hint}}]`. Cheap (codes already match Apollo) but touches 403/409 behavior + orbctl parsing + e2e — don't do speculatively. Context in `docs/reference/ERROR-RESPONSES.md`. |
| **Config editor Save is not atomic — a partial edit can commit silently** | **Found 2026-08-28 while scoping Spike 36 §17 Session 3.** `configitem-editor.js` builds a list of mutations for one Save (root update + per-target updates + creates) and dispatches them with `Promise.all(calls.map(fetch))`. If the 3rd of 5 fails, the first two are **already committed**; the "fail-fast on the first error" loop only stops *reporting*, not writing. The user sees `Server error (500) — try again` with no indication that part of their edit landed. The code comment says parallel is "safe" — true about ordering (no cross-call dependency), but it reads as the stronger atomicity claim. **Same defect class as Spike 36 D11** — but note D11 was **rewritten** before implementation and no longer promises an atomic write path: DGraph gives no cross-root-field transaction (measured — two root fields, the first commits when the second fails), so merge applies items one at a time and records a `merge_attempt` instead. **There is therefore no atomic path to route this through.** What Spike 36 *does* offer as a model is the handling: per-item outcomes recorded, and a partial application made self-correcting rather than silent. **Fix:** make the editor report per-mutation outcomes — "3 of 5 saved, these 2 failed" — instead of a bare 500 that hides what landed. Sequential-with-stop is also strictly better than `Promise.all` here, since it bounds the damage to one item. Independent of Spike 36. |
| **The UNFILTERED change-request queue renders every row, each costing DGraph round-trips** | **Partially fixed 2026-08-29 — the half that mattered.** `GET /api/v1/change-requests` derives staleness per row, and deriving it calls `baseScope`, which calls `collectRelatedOrbIDs` **once per declared orbId** — roughly N x (M + 3) DGraph calls for N requests of M items. **Fixed for FILTERED queries:** `?orbId=` and `?namespace=` now run as jsonb containment in SQL against a GIN index *before* any row is rendered, so the pending-change badge (which fires on every detail view and usually matches nothing) costs zero DGraph calls. `TestListFilter_NonMatchingOrbIDQueryMakesZeroDGraphCalls` pins that. **Still outstanding:** the unfiltered queue page renders all rows and pays the full cost. Acceptable — it is human-opened at a scale of tens, and D13's rule is measure before optimising. **Cheapest next fix, when a number justifies it:** batch the owned-subtree lookup into one aliased GraphQL query per request instead of one per orbId. **Then** the `change_target` projection table D13 holds in reserve. |
| Replace hand-rolled GraphQL request parsing with a real AST parse | **Priority raised 2026-08-29 — it is about to become security-load-bearing.** Spike 36 session 2 puts the approval gate in `writeToDGraph`, and the gate decides what to protect from `knownMutationRe` / `extractOperations`. A regex miss stops being a missing audit row and becomes a **silent authorization bypass**. Session 2 mitigates by failing closed (an unparseable mutation on a gated namespace is refused), but that is a guard rail, not a fix. **Address soon.** The `/graphql` proxy understands mutations via regex + string matching (`knownMutationRe`, `extractOperations`, comment-stripping, the Spike 31 inline-selector guard) — parser-level fragility without parser-level correctness. A real AST parse (`gqlparser`, needs dep sign-off) strengthens authz + stamping together and removes the inline-selector constraint (Spike 31 Option B). Not MVP-urgent (every real caller uses the variable form) but this is the principled fix. Touches the load-bearing `Handle` path — do it deliberately with the Spike-31 tests as the net. |
| **Consolidate the 4 near-identical per-type edit-modal templates into one** | **Found 2026-08-29 while reviewing modal code after a UI polish pass.** `edit-modal-{server,cluster,datacenter,networkdevice}.gohtml` in `web/templates/shared/components/` are ~95% identical — they differ only by an id **prefix** (`srv`/`cluster`/`dc`/`networkdevice`), the title, the reload url/target, and 1–2 `data-*` attrs; the body (a `json-editor` div + Save/Cancel + the two data/targets `<script>` blocks) is byte-identical. The duplication spans **three layers**: 4 templates + 4 handler parse-lines (`server.go`/`cluster.go`/`datacenter.go`/`networkdevice.go`) + per-type JS openers in `orbital.js` (`getElementById('edit-modal-{prefix}-'+id)`). So adding a ConfigItem *parent* family is currently a 5-file copy. **Fix:** one `edit-modal.gohtml` taking `{Prefix, Title, DomID, OrbID, ReloadURL, ReloadTarget, DataAttrs map, EditDataJSON, EditTargetsJSON}` (variable attrs via `{{range $k,$v := .DataAttrs}}`), a shared `EditModalData` struct + helper the four handlers populate, and **one delegated JS opener** keyed on a shared attribute (e.g. `[data-edit-modal]`) replacing the per-type ones — that opener is the real work (needs per-family browser verification; `cluster_test.go` asserts `edit-modal-cluster-`, update it). **Also correct UI.md line 15**: its "adding a ConfigItem family is registry-driven, no JS edits needed" claim is true for a *sub-kind* under an existing parent but **false for a new parent family** today — this consolidation is what makes it true. Not a slot problem (identical body), so it's pure Go-template parameterization. Cheap while it's four families; every new family added before this is another 5-file copy. |

| **`orbId` cross-type uniqueness is unenforced — a written Settled Decision that nothing checks** | **Found 2026-09-03 by reproduction on the real schema (DGraph v25.3.1), not from an incident.** `CLAUDE.md` claimed `orbId` is *"globally unique across every type"*; that line is now corrected. **Plain `@id` on an interface is per-implementing-type**: `addServer(orbId:"dup:collide")` and `addRack(orbId:"dup:collide")` both returned `numUids 1`, and `queryConfigItem` returns both. The shared DQL predicate is `@upsert`-indexed, which prevents concurrent double-writes, but that is a **conflict key, not a uniqueness constraint**, and GraphQL's existence check is type-scoped so a cross-type collision is never looked for. The only app-level duplicate check (`approval/validate.go:117`) is within-changeset. **Blast radius — three places assume the guarantee.** (1) `scopeVersions` (`changerequest_base.go:307`) queries raw DQL — `eq(ConfigItem.orbId, [...])`, **type-agnostic** — and folds rows into `out[orbId] = version`; a collision makes one silently overwrite the other, **non-deterministically**, and `base_hash` (the change-request staleness anchor) becomes ambiguous. (2) **The published API contract**: change-request items take `type` as *optional*, "resolved from `orbId`" (`CHANGE-CONTROL.md:278`, `:291`, `:334`, and the swagger description) — resolution is ambiguous under a collision. (3) **The `/proposed-changes` join**: clients are told to overlay proposals with `proposals[node.orbId].fields[name]` (`CHANGE-CONTROL.md:203`), a map keyed on orbId that collapses two types into one entry. **Why it is not urgent:** latent, not active. The verified audit against the local graph reports **2064 distinct orbIds, 0 held by more than one node** — the `<kind>-` prefix convention is doing its job. Unenforced ≠ violated. **Audit query** (read-only DQL against blue, run before any remedy — also run it against AKS dev and any production graph): `{ dupes(func: has(ConfigItem.orbId)) @groupby(ConfigItem.orbId) { c: count(uid) } }`, then report any group with `c > 1`. **The remedy is NOT retroactive** (reproduced): `@id(interface: true)` (needs v23.1.0+; we run v25.3.1) refuses a *new* cross-type duplicate — `id … already exists for field orbId in some other implementing type of interface ConfigItem` — but a **pre-existing** duplicate survives the schema change untouched and still returns from `queryConfigItem`. So the audit runs first or the change buys nothing for data already in the graph. **Sequencing:** adopting the directive re-indexes `ConfigItem.orbId` across every ConfigItem. If `/admin/schema` blocks for that build (reported, **not independently verified**), it should ride along with the `version: Int! @search` reindex in the CAS backlog item rather than being a second blocking one — a deliberate decision, not a silent fold-in. **Acceptance:** the audit reports zero cross-type duplicates on every graph; a fresh cross-type duplicate is refused; `scopeVersions` cannot return two rows for one orbId. Full investigation (incl. reproductions and provenance markers) was written to `.local/mvcc-investigation.md` §6 — gitignored, so re-derive from the audit query above rather than relying on it. |

### Audit backlog — May 2026 audit, triaged 2026-07-23

> Verified against current code; source finding files deleted. The security audit's two **Critical** findings (unauthenticated `/graphql`; missing readiness probe) and "RBAC defined but not enforced" were already **FIXED** (Spike 11 + single authenticated `/graphql` group + `/healthz`). **No item below is an OSS release-blocker.**

| Item | Notes |
|---|---|
| OIDC config ships real Armada identifiers as defaults | **Pre-OSS deletion pass, not a code condition.** Blank the real tenant/app-ID/allowlist/`admin@armada.ai` defaults to `""` — `config.go:60-72`. The *fail-closed* half is already handled generically by S.16 (`server.go` refuses to boot in prod when `apiAuth` is empty); do NOT add an Armada-specific `== "<tenant-guid>"` boot check (rejected 2026-08-13 — vendor cruft that doesn't serve OSS). Once defaults are empty, a prod deploy that forgets to configure OIDC fails the boot via S.16. (A.1) |
| ~~Rate limiting absent~~ **DONE 2026-08-13** | Per-IP token buckets (in-memory, single-replica), opt-in via `ORBITAL_RATE_LIMIT_ENABLED` (off in dev/AKS-dev, prod enables); tighter bucket on `/user/login`; denials render the `RATE_LIMITED`/429 envelope + `Retry-After`. `server.go`. (S.12) |
| ~~No request body size limit~~ **DONE 2026-08-13** | Global `echomw.BodyLimit(ORBITAL_MAX_REQUEST_BODY, default 10M)` bounds the unbounded `io.ReadAll` on `/graphql`; over-limit renders the `CONTENT_TOO_LARGE`/413 envelope. No file-upload endpoints, so global is safe. `server.go`. (S.7) |
| ~~OIDC-unreachable degrades to no-auth~~ **DONE 2026-08-13** | `server.New` now returns an error and refuses to boot when `!Dev && len(apiAuth)==0` — covers unreachable OIDC discovery, verifier-init failure, and unset issuer in one generic guard. Dev unaffected (`!Dev`-gated). `server.go`. (S.16) |
| ~~No audit on destructive deletes~~ **DONE 2026-08-13** | `BackupHandler.Delete` → `deleteBackup` and `OCI.DeleteArtifact` → `deleteArtifact` management events (actor via `actorFromContext`, `writeAuditEvent`). Test gap: integration guard (`//go:build integration`, needs `make up`) asserting each delete writes its event — add via `newBackupHandler` + `testDB.Event.Query()` when the stack is up. (S.10) |
| ~~MVCC silent-pass on bad version type~~ **DONE 2026-08-13** | `toFloat64` now returns `(float64, ok)`; a malformed `ifVersion` is rejected `400 BAD_USER_INPUT` (a garbage concurrency token is a client error, not a "reload and retry" 409 — deviates from the finding's suggested 409, deliberately). Unit-pinned by `TestToFloat64` !ok cases. `graphql.go`. (A.3) |
| OIDC nonce + constant-time state | Add `nonce` binding; `subtle.ConstantTimeCompare` for state — `oidc.go:95`. (S.14, S.15) |
| Export/restore job-creation TOCTOU | Serialize (mutex or unique partial index); concurrent triggers corrupt scratch-DGraph — `export.go:216-230`. (S.8) |
| Async jobs orphaned on shutdown | Track goroutines (WaitGroup + cancellable ctx); SIGTERM mid-restore can leave DGraph wiped — `server.go`, `restore.go:286`. (S.9) |
| Orb registration not unique | Unique indexes on `datacenter_id` + `public_key` — `ent/schema/orb.go`. (S.11) |
| Job-status columns unindexed | `index.Fields("status")` on ExportJob/Backup/RestoreJob; conflict queries are full table scans. (A.7) |
| Backup zip in `/tmp`, defer-only cleanup | Write to a controlled dir + orphan reaper — `backup.go:627-631`. (A.2) |
| Export scratch-dir leak | `defer os.RemoveAll(jobScratchDir)` after MkdirAll — `export.go:900`. (A.4) |
| `orb scan` fabricates success | Return "not implemented" instead of hardcoded "Found 3 BMC interfaces" until Spike 2 real discovery — `scan.go:16`. (A.6) |
| GET `/export/jobs` writes to DB | Move `StatusStale` marking out of the List handler (HTTP-safety violation; swallows the write error) — `export.go:255-262`. (S.18) |
| OCI push/sign dual credential stacks | Unify ORAS + go-containerregistry creds, or document the coupling — `publisher.go:316-318,371-373`. (A.5) |
| DGraph query string interpolation | Parameterize interpolated queries (`%q`-quoted, low practical risk) — `export.go:963,1049,1223`. (S.17) |
| Orbital templates not embedded | orbital handlers still `ParseFiles` from disk; orb already embeds. Deployment note, non-security — `web/embed.go`. (D.1) |

### Audit log — open backlog (Spike 34 closed 2026-08-26; decisions in `docs/reference/AUDIT.md`)

> Direction, convention, naming, and retention policy are **settled and folded into `docs/reference/AUDIT.md`** (§§ What the audit log is for / Naming / CloudTrail field parity / Retention). What remains is work, not open questions.

| Item | Sev | Notes |
|---|---|---|
| **Rejected mutations write no audit row** | **High** | `graphql.go` gates the audit write on `!hasGQLErrors(respBytes)`, so a mutation that passes authz and is then rejected by DGraph (validation, constraint, schema) leaves **zero trace**. `authorizationDenied` *is* covered separately by `RequireRole` middleware — the hole is post-authz failures. CloudTrail records these; see `AUDIT.md` § CloudTrail field parity. Fix pairs with the `error_code`/`error_message` columns below. |
| **Add the 4 missing CloudTrail fields** | Medium | `event_source` (`graphql`/`rest`/`internal` — currently unknowable), `source_ip_address`, `user_agent` (the standing OWASP gap), `error_code`/`error_message`. All **additive nullable columns**, so safe under ent's append-only auto-migration — not blocked on Spike 27. Make the error fields mandatory top-level, not buried in a response body. |
| **Retention: env var + in-process pruner** | Medium | Decided: `ORBITAL_AUDIT_RETENTION_DAYS`, default `0` = indefinite, operator sets it (orbital is not prescriptive). **Nothing prunes today.** Ship the pruner in-process from day one — NetBox's 90-day default sat inert behind an optional cron and silently deleted history on upgrade when 4.4 activated it. Also surface row count / oldest-record age, since the default is unbounded. |
| `changes[]` absent for creates, bulk adds, multi-op events | Medium | `before` is fetched only for single-entity mutations carrying `id`/`orbId`, so `addServer(input: [...])` produces no diff. NetBox changelogs all three. Cheaper alternative: emit `changes[]` with `before: null` so clients have one code path. |
| Non-orbId-filtered mutations record empty `resource_ids` | Medium | Mutations filtered by another field that select only `{numUids}` are invisible to every per-orbId panel. Documented in `AUDIT.md` § extractResourceIDs. |
| `after` is the mutation input, never a post-mutation re-read | Low | If DGraph coerces or partially applies, the audit asserts something that did not happen. Decide: pay a round-trip, or document it. Leaning document. |
| Out-of-band writes are invisible to API consumers | Low | `dropAll` restore writes zero rows; direct-DQL and seeding bypass the proxy. Documented internally, but nothing tells a **client** the trail has holes — consider a `disclaimer` field like the diff endpoints carry. |
| Subtree audit scope is N calls | Low | Clients traverse Topology for descendant orbIds then pass repeatable `orbId` params (≤128). A schema-level `descendantOrbIds` field would make it one call. Build when a client asks. |


### Track A2 — regression, fix independently of any refactor

| Item | Notes |
|---|---|
| ~~**The cascade-delete endpoint BYPASSES the approval gate**~~ **FIXED 2026-09-03** | **Found and MEASURED 2026-09-03.** `DELETE /api/v1/config-items/:type/:id` planned a cascade and POSTed straight to DGraph via DQL, so it never passed through `writeToDGraph` and `checkApprovalPolicy` never ran. **Reproduction, same entity / same policy / same caller, seconds apart:** with a policy (`allTypes:true`, `bypassRoles:[]`) — `POST /graphql updateDataCenter` → **403 APPROVAL_REQUIRED**; `DELETE` of that entity → **200 `{"deleted":1}`**. A rename refused, a cascade delete not. It also made a documented claim false (*"the single function every DGraph write passes through"* — there was a third path, and it was the destructive one). **Fixed:** `checkPolicyFor(ctx, orbIDs, types, caller)` extracted from `checkApprovalPolicy` so the decision no longer requires a mutation body; `DeleteHandler` takes a `*GraphQL` and calls it after planning, before any write. **The type list is DERIVED from the planned uids, not declared per branch** — hand-tagging nine call sites across three plan builders fails silently the day someone adds a branch, and silent under-gating is the bug being fixed. So a policy protecting only `IdracSettings` now refuses the Server delete that would take it. `?ifVersion=` was added on the same path at the same time. **One trap worth remembering, caught by a test rather than review:** the guards first called `writeError` directly, which returns `c.JSON`'s nil on success — so `if err != nil` never fired, the 409 was written AND the cascade deleted anyway. Guards now return a decision and the handler writes it. Covered by `delete_guard_integration_test.go` (6 tests, incl. the ungoverned negative and the protected-child case), all mutation-verified. |
| ~~The UI stopped sending `ifVersion` — MVCC is off on the direct edit path~~ **FIXED 2026-09-03** | **Found 2026-09-03.** `grep -rin ifversion web/` returns nothing: no client sends it. `b1157ac "Fix orb ui"` (2026-06-20) removed `ifVersion: currentVersion` and `variables.ifVersion = currentVersion` from `orbital.js` while extracting `configitem-editor.js` — the per-page edit modals each passed it, the shared module never carried it forward. **Effect: two edits to the same field from two browsers silently last-writer-wins** — no 409, no warning, both audited as ordinary updates. Not a decision; a refactor casualty under a commit message about orb's UI, and invisible because `ifVersion` is opt-in server-side, so an absent client looks exactly like one that declined. **Nothing caught it:** `TestHandle_IfVersionStrippedBeforeProxy` and the conflict tests assert the server honours `ifVersion` *when supplied*; nothing asserts a client supplies it, and no e2e covers concurrent edits. **Fix:** `configitem-editor.js` declares `$ifVersion: Int` and sends the loaded snapshot's `version`; `withoutStamped` keeps stripping `version` from `set` (top-level variable ≠ set field — do not relax the strip); a 409 renders in the modal leaving the edit intact. Plan: `.local/refactor-ifversion-change-requests.md` § Phase 0 — **ships alone, do not wait on the change-request refactor.** Add an e2e for two concurrent edits, or this regresses the same way again. **Restored in `3c88933` "Restore mvcc on config-item edits in ui"** (ancestor of HEAD). `configitem-editor.js:884,898` now pass `ifVersion` for the root target and each nested target; `:103` guards on `Number.isInteger(ifVersion) && ifVersion > 0` so a missing version degrades to unguarded rather than sending garbage. Verified 2026-09-03: `grep -rin ifversion web/` returns 8 hits, and the Go side reads it in the refactored `writeToDGraph` pre-flight. **This is what keeps the residual MVCC race at LOW severity** — the token is captured at modal-open, so the guard spans the whole time a form sits open, which is where realistic conflicts live. |

### Track B — needs a 15–20 min Opus design session before Sonnet implements

| Item | Notes |
|---|---|
| ~~A supplied `ifVersion` is silently ignored when no single entity resolves~~ **CLOSED 2026-09-03** | **Found** while moving the write pre-flight, and preserved unchanged at the time rather than quietly hardened. `checkIfVersion` compares only when the target row resolved; when it did not (failed before-fetch, multi-type mutation, inline selector) the token was dropped, stripped, and the write proceeded **unguarded with a 200** — a caller that asked for a check and believed it was protected. **Closed without needing the design decision this row was filed for.** The compare-and-swap does not depend on the pre-flight read: a token that cannot be COMPARED in Go is still ENFORCED by DGraph, and a shape that cannot carry the predicate is refused rather than sent. So the fail-open case no longer exists — there is no path where `ifVersion` is present and unenforced. Pinned by `TestCAS_TokenIsStillEnforcedWhenThePreFlightCannotResolveTheEntity` and `TestCAS_MultiTypeMutationCarryingIfVersionIsRefusedNotDropped`, both mutation-verified: restoring the old skip-when-unresolvable behaviour makes them fail. |
| UUIDv4 primary keys should be v7 | Every operational table (`approval_policy`, `audit_event`, `backup`, `divergence_entry`, `export_job`, `orb`, `restore_job`, …) uses `field.UUID("id", uuid.UUID{}).Default(uuid.New)` — that is **UUIDv4, random**. Random keys scatter B-tree inserts across the index instead of appending at the right edge, so pages fragment and the working set grows; time-ordered **UUIDv7** fixes that and gives rough creation ordering for free, which also makes `ORDER BY id` a sane fallback. Current advice (RFC 9562, 2024) is v7 for new database keys; v4 for keys that must not leak a timestamp. **Not urgent** — invisible at orbital's row counts, and it is a schema-wide change, so do it in one pass rather than per table. **Do NOT rewrite existing ids**: v4 and v7 are both valid UUIDs and coexist in one column, so switching the default is enough. `google/uuid` exposes `uuid.NewV7()` (returns an error, unlike `uuid.New`), so the ent `Default` needs a small wrapper that falls back to v4 rather than panicking. Check per table whether an embedded creation timestamp is acceptable before switching — for audit rows it is, since `timestamp` is already a column. |
| DGraph client abstraction | 22+ raw `http.Post` calls across 7 handler files, no timeouts, no pooling. Extract `internal/dgraph/client.go`. **Interface shape is a design decision** (transport vs semantic level) — do NOT implement on Sonnet without a settled design. Primary unlock for Go integration testing. |
| `internal/handler/` god package | ~3,500 lines mixing routing, business logic, DGraph calls, file I/O. Decompose post-MVP in 3 steps: extract `internal/storage/`, then `internal/export/`, then `internal/backup/`. Do NOT start before the MVP feature cut. |
| Registry-driven orbId derivation (kill the `leafSuffix` escape hatch) | **Trigger:** ServerMaintenance feature, 2026-08-13. **Problem:** `configitems.BuildEditTargets` derives every owned-child edit-target orbId as `fmt.Sprintf("%s:%s-%s", namespace, name, leafSuffix(kind))` (`registry.go:469`), where `leafSuffix` (`registry.go` ~518) is a hardcoded `switch`. That only matches the **legacy** `<ns>:<parentName>-<suffix>` shape. New prefix-convention types (`server-maintenance-<serial>`, `network-adapter-<serial>-<FQDD>`) don't fit, so every editor-creatable type needs a hand-written `OverrideEditTargetOrbID` in its page handler — e.g. `server.go` overrides `IdracSettings` (with the *fetched* id) **and** `ServerMaintenance` (with a *constructed* `<ns>:server-maintenance-<serviceTag>`). The correct orbId shape lives in code, so the registry is no longer the single source of truth for orbId. **Why it bites:** for an *optional* child (first-time-create is the common case) the wrongly-derived id is what actually gets written — a silent correctness footgun, not cosmetic. **Fix:** let the `Type` struct declare its orbId pattern so `BuildEditTargets` derives the right id with no override. **Design decision (why Track B):** the pattern needs natural-key inputs the registry doesn't hold (owner serial/serviceTag, FQDD, …). Options: **(a)** `OrbIDKey func(parent map[string]any) string` per-type closure — handlers pass the fetched parent object; handles multi-part keys (network types); **(b)** declarative template + `KeyField` naming the parent field that supplies the key (e.g. `server-maintenance-{key}`, KeyField `serviceTag`) — simpler but weak for multi-part keys; **(c)** just move each construction into a named registry helper (declared once, not re-derived per handler). Recommend (a). **Scope:** `registry.go` (`Type` + `BuildEditTargets`, retire `leafSuffix`); remove the now-redundant `OverrideEditTargetOrbID` calls in `server.go` (+ audit `cluster.go` / any network editor) where the pattern covers them, keeping override only for genuine legacy/fetched-id cases; add a registry test asserting each editor-creatable type derives its documented `DGRAPH.md` orbId. **Acceptance:** adding a new prefix-convention ConfigItem needs ZERO handler orbId override — schema + registry entry is enough (the add-configitem playbook's promise). Pin with a test that derives `ServerMaintenance` → `<ns>:server-maintenance-<serial>` with no handler code. |

### Architecture — requires design discussion before any code

| Item | Notes |
|---|---|
| Orbital HA — pervasive single-replica assumptions | Deployed `replicas: 1`, `strategy: Recreate`. Divergence ingester (`lastIngestedByDC` in-memory), backup scheduler (cron double-fires), publish-history ingester all assume single-leader. Going HA needs leader election / advisory locks / dedicated ingest deployment. **Do NOT scale past `replicas: 1`** — double-ingestion corrupts divergence state. |
| The graphdiff-rendering surfaces build HTML in JavaScript, not Go templates | **Raised 2026-09-01.** The house pattern is a Go template plus `HX-Request` fragment negotiation inside the existing handler — `audit.go`, `backup.go`, `export.go`, `datacenter.go`, `cluster.go`, `networkdevice.go`, restore-jobs, oci-artifacts and divergence-reports all do it. **Five surfaces do not:** the change-request queue, the change-request review page, export preview, artifact compare, and approval-policies. `grep -n "HX-Request" internal/handler/changerequest*.go` returns nothing — those endpoints are JSON-only with no fragment representation. **This is ONE decision, not five.** The four diff-adjacent surfaces share `renderExportPreviewTable` (three callers: `orbital.js:619` export preview, `:2796` change-request diff, `:3522` artifact compare), all consuming the identical flat `changes[]` a single graphdiff core produces. Server-rendering any one of them needs a Go twin of that renderer or the three drift, so the real question is **"should the graphdiff renderer live in Go"** — answer it once and all four convert together. Do NOT convert a single page, and especially not part of one: splitting a page across both patterns is worse than either. **The framing that made this look principled was wrong.** "API-first — orbital's UI is a consumer of its own API, never a privileged server-rendered path" was read as forbidding server-rendered HTML. It does not: CLAUDE.md's own rule is to branch on `HX-Request` **inside the existing handler**, so the JSON contract is untouched and the fragment is a second representation of the same resource. The two conventions never conflicted. **Two concrete costs, both stronger than the consistency argument.** (1) *The client interprets an authorization decision* — `availableActions` is derived server-side from role, authorship, status and prior approvals, and `actionButtons()` in `orbital.js` loops over it to decide which controls exist. It is faithful today and nothing enforces that it stays so; a fragment makes drift structurally impossible. (2) *Escaping is manual* — every interpolation needs an explicit `esc()`, where a Go template escapes by construction. All current call sites are correct, but titles, descriptions and review comments are now free-form user text flowing through string concatenation. **No user-visible payoff**, which is why it is filed rather than done. |
| Storage and Network panels cannot surface a proposed change at all | **Raised 2026-09-02.** The proposed-change surfaces (banner, field marks, tab dots) all key on `data-field-orbid` + `data-field="<name>"` rows, which only exist on **field/value tables** — Server Summary, iDRAC, Maintenance. Storage and Network panels render **lists of child entities** (a row per device: Name / Manufacturer / Capacity / Serial / WWN), so a change request touching a `StorageDevice` or `NetworkInterface` field shows **nothing anywhere on the server tab** — no mark, no dot, and the top-of-tab banner only counts requests without saying where. A reviewer can therefore retype exactly the edit this feature exists to prevent. **Design call:** mark at ROW level ("this device has a proposal", linking to the request) which needs a per-row orbId and a different mark shape than the field tables use; or tab-dot only, so the tab at least says "look in here" without per-row detail. Lean row-level — a list of 24 storage devices with an undifferentiated tab dot is barely better than nothing. Do NOT bolt `data-field` rows onto a list table to reuse the existing renderer: the field tables are field-per-row and these are entity-per-row, and forcing one shape onto both is how the three tables that "must not drift" start drifting. |
| ~~Change-request `before` guards composite fields at WHOLE-VALUE granularity~~ **RETIRED 2026-09-03** | **Raised 2026-09-02**, and gone because the field it describes is gone. `before` sent primitives only, so a String holding JSON was guarded as one opaque value and two proposers editing DIFFERENT keys inside `DataCenter.assetDataV2` collided as a total conflict — a false conflict no re-review could resolve, since `base_values` re-anchored to the other party's whole blob. The client-supplied `before` was removed when `ifVersion` took over the entity-level question, so there is no client assertion to guard at the wrong granularity. **The design call it was filed for is moot**, not deferred: nothing needs to decide between per-key parsing and a better message. Merge-time field protection is unaffected — it reads `base_values`, the server's own ancestor, which was never part of this. |
| A changeset cannot distinguish ASSERTED intent from INCIDENTAL fields | **Raised 2026-09-02.** `set` is target end-state, so `set: {a: 1, b: 2}` where `b` was already `2` is indistinguishable from a deliberate assertion of both — the reviewer cannot tell narrow intent from wide. Mitigated for *display* by `satisfied[]` on `GET /{id}/diff` (already-applied fields render struck through), but the mitigation depends on `base_values`: a request whose snapshot read failed, or one created before the ancestor existed, degrades silently to "everything looks proposed" with no marker saying the comparison was unavailable. **Two candidate fixes:** carry an explicit `asserted` flag per field (honest, but a new API concept and every client must set it), or surface `baseValuesAvailable: false` on the diff response so a client can say *"cannot distinguish"* instead of implying it did. Lean the second — it is one boolean and it removes a silent failure rather than adding a concept. Related: `CHANGE-CONTROL.md` § How we arrived at this shape. |
| Auto-apply DGraph GraphQL schema on startup | Today orbital does NOT apply `schema.graphql` on boot, so a schema-bumping image deploy silently drifts until someone POSTs to `/admin/schema` — the 2026-07-27 AKS burn (v0.0.25 queried `retentionDays`, DGraph still v3 → every cluster 404'd). **Proposed:** apply the schema additively on startup, mirroring ent auto-migrate for PostgreSQL. **Design points:** (1) re-scopes the "schema migration out of MVP" decision; (2) blue-green apply to active/blue on boot; (3) additive-only guard; (4) confirm multi-replica racing is harmless (DGraph serializes); (5) fix the `schema.graphql:6` vs `DGRAPH.md` doc contradiction. Alternatives: kustomize initContainer, GitOps PreSync Job. Triggered by the 2026-07-27 burn. |

---
