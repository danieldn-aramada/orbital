## What

<!-- What does this PR do? One or two sentences. -->

## Why

<!-- Why is this change needed? Link to the spike, issue, or decision. State the intended
behavior explicitly — for agent-assisted work the diff wasn't hand-typed line by line, so
the intent isn't implicit in the effort. Make it explicit here. -->

## How it came together

<!-- Provenance: how this change was arrived at — not who typed it (AI assistance is assumed
across this project, so there's nothing to disclose). Capture the load-bearing framing and
the decisions you made steering it — e.g. "asked for X under constraint Y; redirected when it
drifted to Z" — plus any alternatives you considered and rejected. The curated story, not a
transcript. Skip for trivial changes. -->

## Verification

<!-- What you checked and how you know it works: tests added/run, manual QA, edge cases.
Call out anything you want the reviewer to scrutinize or that you're unsure about — this tells
the reviewer where to spend their attention. -->

## Checklist

- [ ] I have read every changed line and take responsibility for this PR
- [ ] `CLAUDE.md` updated if architectural decisions, conventions, or settled rules changed
- [ ] Schema changes are backwards compatible (if touching `schema/`)
- [ ] Go conventions followed — error wrapping, no globals, thin `cmd/`
